Fixed the AWS POLARIS/scenario Packer bakes so they can complete in a fresh account. `polaris-dc` now injects a per-build DSRM secret (`PKR_VAR_dc_dsrm_password`, masked), matching the GCP pipeline; previously DC promotion refused to bake. `polaris-vm` and `techvault` AMI descriptions now use an ASCII hyphen instead of a Unicode dash, which EC2 `ModifyImageAttribute` rejects (the bake built the image, then failed at the description and rolled back the AMI).
