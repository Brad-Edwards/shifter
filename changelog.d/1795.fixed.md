Fixed the AWS `polaris-dc` Packer bake, which failed at DC promotion with `DC_DSRM_PASSWORD is required`. The base-image build workflow now generates a per-build DSRM (Directory Services Restore Mode) secret and injects it as a masked `PKR_VAR_dc_dsrm_password`, matching the GCP image pipeline; no shared DSRM secret ships in the image.
