Consolidated the GitHub Actions OIDC deploy role's managed IAM policies from 10 (the AWS hard limit of 10 managed policies per role) into 5 by AWS service category - `compute`, `networking`, `data`, `security`, and `management` - in `platform/terraform/global/iam/github-oidc.tf`. Every statement, resource scope, and condition moves verbatim; no permissions are broadened or removed, and the `ci_role_permissions_boundary`, OIDC trust policy, `iam:PolicyArn` attach allowlist, and `iam:PassedToService` constraints are unchanged. The role now has 5 free attachment slots, and `scripts/check_tf_iam_role_naming` fails the build if the role drifts back above 6 managed-policy attachments so future services extend an existing category instead of adding new attachments.
