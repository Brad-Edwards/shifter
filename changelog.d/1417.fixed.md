Forward `SMOKE_TEST_USER_EMAIL` into the portal container during the post-deploy smoke. `run-manage-on-portal` now accepts repeatable `--env KEY=VALUE` flags and renders them as `docker exec -e KEY=VALUE`, so `run_post_deploy_smoke` sees the smoke user's email instead of failing on an unset variable.
