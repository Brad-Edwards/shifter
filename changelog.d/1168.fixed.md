Grant the GitHub Actions deploy role the EventBridge Scheduler permissions (plus `scheduler.amazonaws.com` in the `iam:PassRole` allow-list) needed by the Cognito client-secret rotation reminder, and allow attach/detach of account-owned customer-managed IAM policies. Without these the platform Terraform apply failed in environments where the rotation reminder is enabled (`alarm_email` set), blocking deploys.
