Unblock the platform Terraform apply in environments that enable the optional RDS backup-alerts and Cognito client-secret rotation features (`alarm_email` set). Grant the GitHub Actions deploy role the EventBridge Scheduler permissions (plus `scheduler.amazonaws.com` in the `iam:PassRole` allow-list) the rotation reminder needs, and allow attach/detach of account-owned customer-managed IAM policies. Remove the unsatisfiable confused-deputy conditions from the backup-alerts SNS topic and KMS key policies: RDS runs its `CreateEventSubscription` authorization test-publish through `events.rds.amazonaws.com` before the subscription exists, so any `aws:SourceArn`/`aws:SourceAccount` condition fails it with `SNSNoAuthorization`; the grants stay bounded by a single service principal scoped to the one topic and the one CMK.
