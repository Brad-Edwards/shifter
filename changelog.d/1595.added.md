Added a public documentation site: the deploy and operations docs under `docs/` are now built with mkdocs (Material) and published to GitHub Pages by `.github/workflows/docs.yml` (PRs get a strict build check; pushes to the default branch publish). A new `docs/index.md` landing page indexes the runbooks with "read this when…" guidance and each `docs/dev` runbook links back to it, so mid-tree entry no longer dead-ends (ADR-037; part of #1591).
