GCP deployments can now publish the portal capacity gauges (`Shifter/PortalCapacity`: in-flight requests, worker busy ratio, terminal-session utilization) to Cloud Monitoring, reaching parity with the AWS CloudWatch path (PLAT-002). The emitter selects the publisher by `CLOUD_PROVIDER`; metrics stay optional and fail-soft, never blocking worker boot.
