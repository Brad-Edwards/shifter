Fix the deploy's database-migration step timing out on a cold-instance image pull. It waited with `aws ssm wait command-executed` (a fixed ~100s waiter) while the migrate command docker-pulls the new portal image first, which can exceed 100s, failing the deploy even though the migration succeeds. Poll the command status up to its 900s budget instead.
