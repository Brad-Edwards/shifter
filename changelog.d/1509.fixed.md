Give the GCE range-cell backend's in-range SSH guests (GDC and GCE range VMs) a 900s first-boot SSH-ready budget instead of the 300s EC2/SSM default, so the heavy Polaris host image (full first-boot cloud-init) is reachable before the provisioner times out.
