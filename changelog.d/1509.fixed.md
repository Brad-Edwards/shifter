Wire the GCE range-cell backend's fresh-deploy requirements on GCP: give in-range SSH guests (GDC and GCE range VMs) a 900s first-boot SSH-ready budget instead of the 300s EC2/SSM default (the heavy Polaris host image runs a full first-boot cloud-init and was not reachable in time), and provision the prebaked Windows DC domain Administrator password as a Secret Manager secret rendered into the runtime env as `DC_DOMAIN_PASSWORD_SECRET_ID` (previously unwired on GCP, leaving the DC's set_admin_password step with an empty password).
