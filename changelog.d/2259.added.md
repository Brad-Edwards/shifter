Add `sauron` GCP tenant scaffolding: the `environments/sauron` Terraform root,
`cicd-oidc`/`github-runner` `sauron.tfvars`, the `overlays/sauron` kustomize
overlay, its `validation-inventory.yaml` root registration, and `sauron` in the
`packer-gcp.yml` / `packer-gcp-validate.yml` environment enums (#2259).
