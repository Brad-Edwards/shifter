CTF invite links are one-time credentials: onboarding is deferred until token exchange, consumed tokens cannot be reused, and existing platform accounts must sign in through normal login before the invite enrolls them instead of granting passwordless full-account access via the email link.
