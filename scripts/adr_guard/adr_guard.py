#!/usr/bin/env python3
"""Repo-native ADR enforcement checks.

Executable compatibility facade for the ``adr_guard`` package. The check
logic lives in per-family modules under this directory (``checks/*``) plus
the shared kernels ``_common`` / ``_workflow_model`` and the ``_registry`` /
``_cli`` wiring; this module is the CLI entry point and re-exports the full
public surface so ``python3 scripts/adr_guard/adr_guard.py`` and the tests
that load this file by path keep working unchanged."""

from __future__ import annotations

import sys
from pathlib import Path

# Bootstrap the package once: make this directory importable so `_guard`
# resolves both when run as __main__ (sys.path[0] is this dir) and when a test
# loads this file via importlib.util.spec_from_file_location (which does not add
# the dir). All internal wiring uses package-relative imports inside `_guard`.
_PKG_DIR = str(Path(__file__).resolve().parent)
if _PKG_DIR not in sys.path:
    sys.path.insert(0, _PKG_DIR)

from _guard._common import (  # noqa: E402
    LAYERS,
    REPO_ROOT,
    REQUIRED_EXCEPTION_KEYS,
    Violation,
    _ADR_GUARD_PATH,
    _BOUNDARY_MOCK_BASE_REF_ENVS,
    _boundary_mock_base_reference_candidates,
    _boundary_mock_fallback_reference_candidates,
    _git_text,
    _git_tracked_all,
    _load_json_yaml,
    _normalize_files,
    _parse_iso_date,
    _read_text_safe,
    _repo_relative,
    _walk_all_files,
    exception_is_active,
    exception_matches,
    filter_excepted_violations,
    get_changed_files,
    is_guard_source_path,
    load_adr_exceptions,
    validate_adr_exceptions,
)
from _guard._workflow_model import (  # noqa: E402
    _CORE_WORKFLOW_PATH,
    _DW_CANONICAL_REPOSITORY,
    _DW_EXPR_TOKEN,
    _DW_EXPR_WRAPPER,
    _DW_RESULT_REF,
    _DW_REUSABLE_WORKFLOW_PATHS,
    _DwExprError,
    _DwParser,
    _DwShapeError,
    _ENGINE_WORKFLOW_PATH,
    _GCP_DEV_WORKFLOW_PATH,
    _PLATFORM_WORKFLOW_PATH,
    _RANGE_WORKFLOW_PATH,
    _SELF_HOSTED_CLASS_LABELS,
    _dw_call_function,
    _dw_evaluate_env,
    _dw_evaluate_if,
    _dw_extract_set_environment_script,
    _dw_get_job,
    _dw_glob_to_regex,
    _dw_is_self_hosted,
    _dw_job_denied_on_pull_request,
    _dw_job_denied_when_upstream,
    _dw_job_if,
    _dw_job_runs_when_eligible,
    _dw_jobs,
    _dw_load_workflow,
    _dw_loose_eq,
    _dw_normalize_expr,
    _dw_parse_paths_filter,
    _dw_path_matches_any,
    _dw_result_guarded_upstreams,
    _dw_runs_on,
    _dw_tokenize,
    _dw_truthy,
    _dw_unwrap_expr,
    _dw_upstream_gating_violations,
)
from _guard.checks.adr_registry import (  # noqa: E402
    RANGE_SUBSTRATE_DEFERRED_ADAPTERS,
    RANGE_SUBSTRATE_INITIAL_ADAPTERS,
    RANGE_SUBSTRATE_ISSUE_REFERENCES,
    RANGE_SUBSTRATE_OPERATIONS,
    RANGE_SUBSTRATE_RESOURCES,
    REQUIRED_ADR_KEYS,
    REQUIRED_INTERFACE_CONTRACTS,
    _check_adr_entry,
    _registry_violation,
    _validate_exact_string_members,
    check_adr_registry,
    load_adr_registry,
    validate_interface_contract,
)
from _guard.checks.layer_imports import (  # noqa: E402
    CLOUD_ROOTS,
    CLOUD_SKIP_FILES,
    CYBERSCRIPT_ALLOWED_LAYER,
    CYBERSCRIPT_IMPORT_PATTERN,
    IMPORT_PATTERN,
    _LAYER_POLICY_REL,
    _PLATFORM_REL,
    _SETTINGS_REL,
    _classified_packages,
    _is_public_facade_descendant,
    _iter_yaml_section,
    _local_appconfig_packages,
    _parse_installed_apps,
    _symbol_facade_violations,
    check_cloud_factory_seam,
    check_cross_layer_model_imports,
    check_installed_apps_classified,
    check_layer_imports,
    is_import_allowed,
    iter_facade_symbol_imports,
    iter_layer_files,
    iter_private_facade_imports,
    load_allowed_imports,
    load_allowed_symbols,
    load_classification,
)
from _guard.checks.documentation import (  # noqa: E402
    DOCS_COVERAGE_MANIFEST,
    DOCS_COVERAGE_RULE_ID,
    DOC_PATHS,
    GUARDRAIL_FILES,
    GUARDRAIL_PREFIXES,
    _AGENT_ATTRIBUTION_SCAN_SKIP,
    _DOCS_EXCLUDED_PART,
    _MARKDOWN_LINK_PATTERN,
    _collect_index_link_slugs,
    _doc_path_is_excluded,
    _is_docs_file,
    _is_guardrail_file,
    _normalize_doc_slug,
    check_documentation_coverage,
    check_guardrail_docs,
    check_no_agent_attribution,
)
from _guard.checks.mcp_policy import (  # noqa: E402
    _BLANK_KEEP_NEWLINES,
    _CHILD_PROCESS_IMPORT,
    _EXEC_SYNC_ALIAS,
    _REJECT_UNAUTH_FALSE,
    _SHELL_TRUE_SPAWN,
    _blank_for,
    _build_call_site_pattern,
    _consume_block_comment,
    _consume_code,
    _consume_line_comment,
    _consume_string,
    _scan_to_closing_quote,
    _strip_block_comment,
    _strip_js_comments_and_strings,
    _strip_js_comments_only,
    _strip_line_comment,
    check_mcp_no_shell_exec,
    check_mcp_ops_tls_strict,
)
from _guard.checks.k8s_security import (  # noqa: E402
    HELM_CHART_DIR,
    HELM_VALUES_FILES,
    K8S_BASE_DEPLOYMENT_DIR,
    _as_network_policy_violations,
    _broad_egress_network_policy_violations,
    _check_container_basic_fields,
    _check_container_capabilities,
    _check_container_identity,
    _check_container_seccomp,
    _check_k8s_container_security,
    _check_k8s_pod_security,
    _coerce_container_sc,
    _collect_shifter_namespaces,
    _default_deny_network_policy_namespaces,
    _destination_ip_block_cidr,
    _document_namespace,
    _effective_field,
    _is_default_deny_network_policy,
    _is_real_int,
    _is_shifter_namespace,
    _iter_egress_destinations,
    _iter_yaml_documents,
    _missing_default_deny_network_policy_violations,
    _network_policy_name,
    _network_policy_violation,
    _render_chart_for_validation,
    _resolve_pod_sc,
    _resolve_pod_spec,
    _scan_targets,
    _shifter_network_policy_docs,
    _v,
    _validate_base_files,
    _validate_chart_renders,
    _validate_containers_list,
    _validate_deployment_documents,
    _validate_network_policy_base_files,
    _validate_network_policy_chart_renders,
    _validate_network_policy_documents,
    check_k8s_deployment_security_context,
    check_k8s_network_policy_coverage,
)
from _guard.checks.secret_hygiene import (  # noqa: E402
    _BLOCK_COMMENT_PATTERN,
    _GENERATED_ARTIFACT_ROOTS,
    _NON_SECRET_NAME_SUFFIXES,
    _SECRET_ASSIGNMENT_PATTERN,
    _SECRET_BLOCK_OPEN_PATTERN,
    _SECRET_ENV_PLACEHOLDERS,
    _SECRET_ENV_ROOTS,
    _SECRET_ENV_SUFFIX,
    _SECRET_HEREDOC_PATTERN,
    _SECRET_NAME_GROUP,
    _SECRET_VAR_PATTERN,
    _STRING_LITERAL_PATTERN,
    _TFVARS_SCOPE,
    _balance_scan,
    _block_assignment_has_literal,
    _block_depth_scan,
    _collect_tfvars_candidates,
    _find_balanced_close_index,
    _find_block_close_index,
    _flagged_secret_var,
    _generated_artifact_match,
    _git_tracked_under_roots,
    _git_tracked_under_roots_for_secret_env,
    _is_bootstrap_authcode_artifact,
    _is_line_commented,
    _is_polaris_operator_run_artifact,
    _is_public_material_name,
    _is_secret_env_in_scope,
    _is_synthetic_placeholder,
    _is_terraform_plan_artifact,
    _iter_artifact_candidates,
    _iter_secret_env_candidates,
    _lines_have_string_literal,
    _scan_secret_env_file,
    _scan_tfvars_file,
    _scrub_line,
    _strip_hcl_comments,
    _strip_trailing_line_comment,
    _walk_filesystem_artifacts,
    _walk_filesystem_secret_env,
    _wrapped_rhs_has_literal,
    check_no_plaintext_secrets_in_tfvars,
    check_no_populated_secret_env_files,
    check_no_tracked_generated_artifacts,
)
from _guard.checks.deploy_workflow import (  # noqa: E402
    _ACTION_PIN_CHECK,
    _ACTION_PIN_OCI_DIGEST,
    _ACTION_PIN_RULE,
    _ACTION_PIN_SHA40,
    _CLOUD_AUTH_ACTIONS,
    _DEPLOY_WORKFLOW_PATH,
    _ENGINE_BOOTSTRAP_INPUT,
    _ENGINE_TASKDEF_STEP,
    _FAIL_LOUD_CHECK,
    _FAIL_LOUD_RULE,
    _GITHUB_OIDC_TF_PATH,
    _GUAC_STABILIZE_STEP,
    _LOCAL_AUTO_TFVARS,
    _PLAN_SCOPE_CHECK,
    _PLAN_SCOPE_RULE,
    _PORTAL_DEPLOY_HELPER_PATH,
    _PORTAL_DEPLOY_MODE_CHECK,
    _PORTAL_DEPLOY_MODE_RULE,
    _PORTAL_DEV_OUTPUTS_PATH,
    _PORTAL_IMAGE_BUILD_INPUT,
    _PORTAL_IMAGE_DEPLOY_CONDITION,
    _PORTAL_IMAGE_OUTPUT,
    _PORTAL_IMAGE_REQUIRED_GLOB,
    _PORTAL_PROD_OUTPUTS_PATH,
    _PR_GATE_SKIPPED_QUALITY_GUARD,
    _QUALITY_GUARDRAIL_DOCS_REQUIRED_GLOBS,
    _QUALITY_NON_DOCS_REQUIRED_GLOBS,
    _QUALITY_ONLY_OUTPUT,
    _QUALITY_ONLY_REQUIRED_GLOBS,
    _QUALITY_PREDICATE,
    _QUALITY_RELEVANT_CONDITION,
    _QUALITY_RELEVANT_OUTPUT,
    _QUALITY_SKIP_TESTS_IMMUNE_JOB_NAMES,
    _QUALITY_SKIP_TESTS_IMMUNE_JOB_SUFFIXES,
    _QUALITY_WORKFLOW_PATH,
    _RUNNER_EXPOSURE_CHECK,
    _RUNNER_EXPOSURE_RULE,
    _SKIP_TESTS_FORBIDDEN_MARKERS,
    _SKIP_TESTS_LITERAL,
    _TERRAFORM_PLAN_FILE,
    _TFVARS_RENDER_CHECK,
    _TFVARS_RENDER_JOBS,
    _TFVARS_RENDER_RULE,
    _TF_CONSUMING_SUBCOMMANDS,
    _action_pin_violation,
    _active_line_contains,
    _block_contains_glob,
    _check_deploy_concurrency_queues_apply_runs,
    _check_deploy_workflow_plan_routing,
    _check_deploy_workflow_portal_image_routing,
    _check_deploy_workflow_quality_only_routing,
    _check_deploy_workflow_skip_tests_policy,
    _check_engine_task_family_fails,
    _check_guacamole_timeout_fails,
    _check_platform_build_portal_image_gate,
    _check_portal_deploy_helper,
    _check_portal_deploy_mode_outputs,
    _check_portal_deploy_mode_workflow,
    _check_quality_workflow_skip_tests_contract,
    _check_saved_plan_apply_contract,
    _check_terraform_plan_lock_timeout,
    _check_terraform_workflow_integrity,
    _deploy_plan_scope_relevant,
    _deploy_runner_exposure_relevant,
    _dw_iter_uses_refs,
    _dw_iter_workflow_files,
    _dw_job_is_cloud_credentialed,
    _dw_job_steps,
    _dw_permissions_grant_id_token,
    _dw_step_uses,
    _dw_step_uses_cloud_auth,
    _dw_uses_is_sha_pinned,
    _dw_workflow_is_cloud_credentialed,
    _extract_job_if,
    _fail_loud_relevant,
    _fail_loud_violation,
    _filter_globs,
    _is_terraform_consuming_command,
    _line_removes_tfplan,
    _noncomment_contains,
    _paths_filter_block,
    _plan_scope_violation,
    _platform_app_source_globs,
    _portal_deploy_mode_relevant,
    _portal_deploy_mode_violation,
    _quality_job_is_skip_tests_immune,
    _quality_workflow_job_names,
    _runner_exposure_violation,
    _should_check_plan_scope_file,
    _terraform_apply_uses_saved_plan,
    _terraform_plan_has_lock_timeout,
    _terraform_plan_writes_saved_plan,
    _tfvars_render_violation,
    _tfvars_render_violations_for_workflow,
    _workflow_action_pin_relevant,
    _workflow_job_block,
    _workflow_step_block,
    _writes_local_auto_tfvars,
    check_deploy_runner_exposure,
    check_deploy_verification_fail_loud,
    check_deploy_workflow_plan_scope,
    check_github_oidc_no_admin_access,
    check_platform_renders_deploy_tfvars,
    check_portal_deploy_mode_source_of_truth,
    check_workflow_action_sha_pinning,
)
from _guard.checks.boundary_mock import (  # noqa: E402
    _BOUNDARY_MOCK_BASELINE_PATH,
    _BOUNDARY_MOCK_BOUNDARY_SEGMENTS,
    _BOUNDARY_MOCK_CHECK_NAME,
    _BOUNDARY_MOCK_RULE,
    _BOUNDARY_MOCK_SKIP_PARTS,
    _BoundaryPatchSite,
    _boundary_mock_scope,
    _boundary_mock_violation,
    _check_boundary_mock_baseline_non_growth,
    _collect_mock_aliases,
    _first_party_python_roots,
    _git_tracked_python_files,
    _has_boundary_mock_skip_part,
    _is_allowed_boundary_patch_target,
    _is_boundary_mock_test_path,
    _is_first_party_internal_patch_target,
    _is_mock_patch_func,
    _is_mock_patch_object_func,
    _iter_boundary_patch_sites,
    _iter_repo_python_files,
    _load_boundary_mock_baseline,
    _load_boundary_mock_reference_baseline,
    _name_chain,
    _parse_boundary_mock_baseline,
    _patch_object_target,
    _resolve_imported_name,
    _walk_python_files,
    check_boundary_mock_policy,
)
from _guard.checks.complexity import (  # noqa: E402
    PYTHON_COMPLEXITY_GATE_PYPROJECTS,
    PYTHON_COMPLEXITY_THRESHOLD,
    _BACKLOG_DOC_PATH,
    _CHECK_NAME,
    _DEF_NAME_PATTERN,
    _HOOK_FILES_LINE_PATTERN,
    _HOOK_ID_PATTERN,
    _NOQA_BARE_PATTERN,
    _NOQA_C901_PATTERN,
    _NOQA_SCAN_SKIP_PARTS,
    _PRECOMMIT_CONFIG_PATH,
    _RUFF_HOOK_ID_PATTERN,
    _RULE_R1,
    _RULE_R2,
    _any_selector_covers_c901,
    _check_backlog_reconciliation,
    _check_canonical_pyproject,
    _check_ignore_field,
    _check_max_complexity,
    _check_per_file_ignores,
    _check_precommit_consistency,
    _check_select,
    _classify_noqa_line,
    _classify_sentinel_noqa,
    _is_change_relevant,
    _load_lint_section,
    _parse_complexity_backlog,
    _ruff_hook_paths_from_precommit,
    _scan_file_for_noqa,
    _scan_noqa_c901_sites,
    _selector_covers_c901,
    _violation_r1,
    _violation_r2,
    check_python_complexity_gate,
)
from _guard.checks.cloud_identifiers import (  # noqa: E402
    _ACCOUNT_ID_RE,
    _ACCT_BUCKET_RE,
    _IAC_IP_SUFFIXES,
    _IDENTIFIER_CHECK,
    _IDENTIFIER_RULE_ID,
    _IDENTIFIER_SKIP_BASENAMES,
    _IDENTIFIER_SKIP_SUFFIXES,
    _IDENTIFIER_SKIP_SUFFIX_LOCK,
    _IPV4_RE,
    _MC_FLAG_CHECK,
    _MC_FLAG_RE,
    _MC_FLAG_RULE_ID,
    _MC_FLAG_SCAN_SUFFIXES,
    _MC_RUNTIME_ROOTS,
    _PUBLIC_IP_ALLOW_NETS,
    _SUBNET_ID_RE,
    _SYNTHETIC_ACCOUNT_IDS,
    _UUID_BUCKET_RE,
    _VPC_ID_RE,
    _identifier_skip,
    _identifier_violation,
    _is_flaggable_public_ip,
    _is_mc_runtime_path,
    _iter_identifier_candidates,
    _iter_mc_flag_candidates,
    _mc_flag_is_placeholder,
    _mc_flag_violation,
    _scan_iac_public_ips,
    _scan_identifier_file,
    _scan_identifier_line,
    _scan_mc_flag_file,
    check_mission_control_no_flag_literals,
    check_no_live_cloud_identifiers,
    check_no_terraform_operational_placeholders,
)
from _guard.checks.published_contract import (  # noqa: E402
    _PUBLISHED_CONTRACT_CHECK,
    _PUBLISHED_CONTRACT_DIR,
    _PUBLISHED_CONTRACT_ENFORCE_ENV,
    _PUBLISHED_CONTRACT_RULE,
    _PUBLISHED_CONTRACT_SNAPSHOT_RE,
    _published_contract_enforced,
    _published_contract_snapshot_diff,
    _published_contract_snapshot_names,
    _published_contract_violation,
    check_published_contract_snapshots_immutable,
)
from _guard.checks.quality_ownership import (  # noqa: E402
    _QUALITY_ADVISORY_JOBS,
    _QUALITY_CHECK,
    _QUALITY_CONTRACT_REL,
    _QUALITY_MATRIX_OUTPUT,
    _QUALITY_RESPONSIBILITIES,
    _QUALITY_RULE,
    _QUALITY_WORKFLOW_REL,
    _load_quality_module,
    _quality_eval_if,
    _quality_job_reachable,
    _quality_matrix_reachable,
    _quality_output_wiring_violations,
    _quality_ownership_completeness,
    _quality_package_reconciliation,
    _quality_probe_path,
    _quality_routing_reachability,
    _quality_strip_if,
    check_quality_path_ownership,
)
from _guard._registry import (  # noqa: E402
    CHECKS,
    CHECK_LEVELS,
)
from _guard._cli import (  # noqa: E402
    _parse_args,
    _print_text,
    _selected_files,
    main,
)
from _guard.checks import (  # noqa: E402
    adr_registry,
    boundary_mock,
    cloud_identifiers,
    complexity,
    deploy_workflow,
    documentation,
    k8s_security,
    layer_imports,
    mcp_policy,
    published_contract,
    quality_ownership,
    secret_hygiene,
)


if __name__ == "__main__":
    sys.exit(main())
